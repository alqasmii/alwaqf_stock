# أسعار يدوية للاستخدام عند فشل الجلب التلقائي من MSX
# Manual price overrides (used as fallback when live scraping fails)
# Format: TICKER=PRICE
# Example: OQEP=0.466

# قم بتحديث هذه الأسعار يدوياً من موقع سوق مسقط
# Update these prices manually from https://www.msx.om/
OQEP=0.466
OQPI=0.218
